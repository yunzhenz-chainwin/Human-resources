# Pin the line endings per file type instead of trusting each clone's
# core.autocrlf config.
*.ps1   text eol=crlf
*.bat   text eol=crlf

# Never let line-ending conversion near these.
*.docx  binary
*.xlsx  binary
*.pdf   binary
*.db    binary
*.png   binary
*.jpg   binary
*.ico   binary
