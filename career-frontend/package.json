{
  "name": "talentbridge-career",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native",
    "build": "vue-tsc --noEmit && vite build --configLoader native",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview --configLoader native"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vue-tsc": "^3.1.0"
  }
}
