# 09. 開發進度

最後更新：2026-07-13

## 目前狀態

- 交付期限已調整為 2026/07/31，範圍改為內部展示／試用 MVP。
- Phase 0：需求確認與開發並行；仍待 HR、IT 提供欄位簽核、真實去識別履歷與部署環境。
- 今日完成線：Phase 1 骨架、SQLAlchemy/Alembic 與第一版核心 migration。

## 已完成

- 建立 `backend/` FastAPI 應用與 `/api/v1/health` 健康檢查。
- 建立後端測試與 lint 設定。
- 建立 `frontend/` Vue 3 + TypeScript + Vite 入口。
- 建立 PostgreSQL、Redis、後端與前端的 Docker Compose 定義。
- 建立環境變數範本與版本控制忽略清單。

## 下一批任務

1. 建立 SQLAlchemy 2、Alembic 與第一版 schema migration。
2. 實作帳密登入、JWT refresh token 與三種角色 RBAC。
3. 實作使用者、部門與角色管理 API。
4. 建立前端登入頁與依角色顯示的導覽骨架。

## 外部待確認

- HR：確認 `docs/03-資料庫設計.md` 的候選人欄位。
- HR：提供 104、1111 與一般履歷各至少 10 份去識別樣本。
- IT：確認正式部署主機是否可使用 Docker、SMTP 與檔案儲存位置。
